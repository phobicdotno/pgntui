# Bedtime Recursive Audit — Summary (2026-06-04)

Branch: main @ 1f3795d (v0.2.1) → bumping toward 0.3.0 after fixes
Audits A/B/C/D dispatched as 4 read-only reviewers covering distinct axes.

## Punch list — fix dispatch groups

### Group F1: PyInstaller spec — Critical
| ID | File | Issue |
|---|---|---|
| C-3 / D-C1 | pgntui.spec | examples/ missing from datas → `--example` crashes in binary |
| D-C2 | pgntui.spec | entry_points metadata not bundled → actisense driver dead in binary |

### Group F2: Concurrency + Lifecycle (drivers, app shutdown)
| ID | File | Issue |
|---|---|---|
| A-1 | app.py | Worker not cancelled before exit() — orphaned thread |
| A-2 | app.py | `_writer` race between worker write and main close |
| A-3 | drivers/actisense.py | `read_frames()` infinite loop, no stop event |
| A-4 | drivers/replay.py | `close()` doesn't track open file handle |
| A-5 | __main__.py | Ctrl+C doesn't close writer → recording tail lost |
| A-6 | signals/widgets.py | No thread-safety docstring on `update_value` |
| C-7 | app.py | `_views` property type hole via `__dict__.setdefault` |

### Group F3: Decoder + Router + Signal types
| ID | File | Issue |
|---|---|---|
| B-1 | decode/canboat.py | `Offset` never applied — 23 AC power fields 2 GW off |
| B-3 | signals/widgets.py | EMA formula direction flipped vs standard; needs docstring |
| B-4 | decode/router.py | Instance match silent cross-contamination when frame has no Instance |
| B-5 | decode/canboat.py | `_FIELD_ALIASES` discoverability — needs docstring + listing |
| C-2 | signals/base.py | Threshold fields not coerced to float in loader |

### Group F4: Fast-packet reassembly — Critical for real NMEA use
| ID | File | Issue |
|---|---|---|
| B-2 | decode/* | NO multi-frame PGN reassembly — GNSS/AIS/wind silently broken |

### Group F5: Recording fidelity
| ID | File | Issue |
|---|---|---|
| B-6 | drivers/base.py, recording/* | Frame missing priority/destination; lossy roundtrip |
| C-4 | recording/writer.py | Text mode produces CRLF on Windows |

### Group F6: Platform paths + Config + Packaging metadata
| ID | File | Issue |
|---|---|---|
| C-1 | config.py, __main__.py | `~/.config/pgntui` non-standard on Windows; use platformdirs |
| C-5 | config.py | TOML syntax errors produce raw traceback |
| C-6 | pyproject.toml | `textual>=0.80` lower bound too loose (need 8.0+) |
| D-I3 | pyproject.toml | Missing `[project.urls]` |
| D-I4 | README.md | Sparse; dead spec link breaks PyPI page |
| D-I5 | packaging/README.md | New: document manual homebrew/winget release steps |

### Group F7: CI/Release + Validation + Test flake
| ID | File | Issue |
|---|---|---|
| D-I1 | .github/workflows/release.yml | No timeout-minutes; macos-13 hangs block release |
| D-I2 | .github/workflows/release.yml | No CI gate before PyPI publish |
| D-I6 | tests/test_replay_mode.py | Timing assertion flaky risk on loaded CI |
| B-7 | containers/loader.py | Duplicate (row,col,w) placement not detected |
| B-8 | themes/loader.py | Gradient stops not validated (empty/single/malformed hex) |

## Out of scope (deferred or no fix needed)
- A-7 — replay leftover_delay is correct (P3, no fix)
- B-5 (alias discoverability) — documentation only
- C-8 — float locale (CPython safe)

## Dispatch plan
7 parallel fix agents working on disjoint file sets (with pull-rebase retry on push). Final orchestrator agent bumps to 0.3.0, runs full suite, tags + pushes.
